{
  "version": "v1",
  "hooks": [
    {
      "name": "Commit After Task Completion",
      "trigger": "PostTaskExec",
      "action": {
        "type": "agent",
        "prompt": "A spec task was just completed. Stage all modified files related to this task and create a git commit with a descriptive commit message. The commit message should follow conventional commits format (e.g. \"feat:\", \"fix:\", \"refactor:\") with a concise subject line summarizing what the task accomplished, and a body if needed for additional context. Do not push — only commit locally."
      }
    }
  ]
}